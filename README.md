# rbc-ac-assignment
MyProject


This is a Maven project to automate US 03 – Be able to add a new pet

I have created a Maven Project to automate using Selenium with TestNG using Page Object model Framework


Procedure to execute the Automation test:

Entire project is committed in GitHub. 
Any Java IDE can be used, Eclipse or Iintellij.
Build the maven project first so all the dependency will be downloaded in the project automatically.
In this project following are the few dependencies
<artifactId>maven-surefire-plugin</artifactId>
<artifactId>selenium-java</artifactId>
<artifactId>testng</artifactId>
Need to make sure that Firefox version is 46.0 Right 
click project run as Maven testFor reporting
purpose I am using TestNG. 
